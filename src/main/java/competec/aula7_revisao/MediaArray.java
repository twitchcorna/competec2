package competec.aula7_revisao;

// Calcule e exiba a média dos números de um array

public class MediaArray {
    public static void main(String[] args) {
        int[] numeros = {10, 20, 30, 40, 50}; // Modifique os valores conforme necessário
        int media = (numeros[0] + numeros[1] + numeros[2] + numeros[3] + numeros[4]) / numeros.length;
      System.out.println("a media e " + media);

        // Calcule a média dos números
    }
}
